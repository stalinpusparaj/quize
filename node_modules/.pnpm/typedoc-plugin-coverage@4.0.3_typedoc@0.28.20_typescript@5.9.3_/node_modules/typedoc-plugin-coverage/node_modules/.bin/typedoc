#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")
basedir_win="$basedir"
exe=""
msys=""

case `uname -a` in
  *CYGWIN*|*MINGW*|*MSYS*)
    if command -v cygpath > /dev/null 2>&1; then
      basedir_win=`cygpath -w "$basedir"`
    fi
    exe=".exe"
    msys="true"
  ;;
  *WSL2*)
    if command -v wslpath > /dev/null 2>&1; then
      basedir_win="$(wslpath -w "$basedir" 2> /dev/null)"
      if [ $? -ne 0 ] || [ -z "$basedir_win" ]; then
        basedir_win="$basedir"
      else
        exe=".exe"
      fi
    fi
  ;;
esac

if [ -n "$exe" ] && [ -x "$basedir/node.exe" ]; then
  exec "$basedir/node.exe"  "$basedir_win/../../../typedoc/bin/typedoc" "$@"
elif [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../typedoc/bin/typedoc" "$@"
elif command -v node >/dev/null 2>&1; then
  exec node  "$basedir/../../../typedoc/bin/typedoc" "$@"
elif [ -n "$exe" ] && command -v node.exe >/dev/null 2>&1; then
  exec node.exe  "$basedir_win/../../../typedoc/bin/typedoc" "$@"
else
  exec node  "$basedir/../../../typedoc/bin/typedoc" "$@"
fi
# cmd-shim-target=/Users/stalin/Downloads/Asset-Manager/node_modules/.pnpm/typedoc-plugin-coverage@4.0.3_typedoc@0.28.20_typescript@5.9.3_/node_modules/typedoc/bin/typedoc
